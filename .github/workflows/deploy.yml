name: Deploy to Vercel

on:
  push:
    branches: [main]

jobs:
  deploy:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: npx vercel --prod --token=${{ secrets.VERCEL_TOKEN }}
        env:
          VERCEL_ORG_ID: team_UMpbfyWQkVANe9gFCViO7mtc
          VERCEL_PROJECT_ID: prj_0aOy12t5KiJZ3HMpkLlkO4N7y33F
